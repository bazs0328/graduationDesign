# GradTutor minimal setup (recommended for non-technical users)
#
# 1) Keep both providers as auto unless you have a specific requirement.
# 2) Fill at least one API key.
# 3) Most advanced tuning is available in frontend:
#    设置中心 -> 高级设置与诊断 -> 系统高级参数（可编辑）.
# 4) .env.advanced.example is only a fallback for headless/server deployments.

LLM_PROVIDER=auto
EMBEDDING_PROVIDER=auto
OPENAI_API_KEY=
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=
DATA_DIR=data
AUTH_REQUIRE_LOGIN=true
